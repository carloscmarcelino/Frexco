export type Colors = {
  orange: string;
  purle1: string;
  purle2: string;
  white: string;
};

export type FontWeight = {
  regular: number;
  bold: number;
};

export type FontSize = {
  sm: string;
  md: string;
  lg: string;
};

export type LineHeight = {
  sm: string;
  md: string;
  lg: string;
};
