import { extendTheme } from '@chakra-ui/react';

import '@fontsource/montserrat';

export const theme = extendTheme({
  colors: {
    orange: '#e80',
    purle1: '#87f',
    purle2: '#76f',
    white: '#fff',
  },
  fonts: {
    body: 'montserrat',
  },
});
